{
  "name": "sherpa",
  "private": true,
  "scripts": {
    "test": "pnpm -r run test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome check --write . && prettier --write \"**/*.md\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "husky": "^9.1.7",
    "prettier": "^3.8.1"
  }
}
